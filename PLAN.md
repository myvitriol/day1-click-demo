# CLICK 1-Day Demo — 기획 (v1.0)

> 현장에 노트북 한 대를 들고 가서, 웹페이지 하나로 **커넥터 체결음을 그 자리에서 잡아 보이는** 영업용 데모.
> 데이터 수집 계약 **전에** 가치를 증명하는 것이 목적이다. 2026-08-21 1차 베타 · 08-31 최종.

---

## 1. 굵직한 기능 — 6개

| # | 기능 | 한 줄 |
|---|---|---|
| **F1** | **마이크 연결 확인** | 외장 인터페이스를 자동으로 잡고, 무엇이 96 kHz로 열렸는지 화면 상단에 항상 표시 |
| **F2** | **실시간 파형** | 소리의 크기. 배경 소음 띠 대비 얼마나 솟았는지가 곧 SNR — 숫자를 몰라도 높이로 읽힌다 |
| **F3** | **스펙트로그램** | 소리의 높낮이. 파형과 같은 시간창. 체결음은 전 대역 세로줄 하나로 보인다 |
| **F4** | **소리의 지도** | 기존 체결음 무리 · 그 밖의 소리 · 방금 그 소리(★). 배경 점을 누르면 그 소리가 재생된다 |
| **F5** | **freeze & 해부** | 체결음을 잡으면 세 화면이 그 순간에서 멈춘다. 발표자가 재개할 때까지 한 건을 뜯어본다 |
| **F6** | **판정 기준 3단 dial** | 느슨/보통/엄격. 돌리면 이 방에서 해본 시험이 그 기준으로 즉시 재채점된다 |

**F1~F4는 모델 없이 신호처리만으로 돈다.** 모델이 죽어도 화면은 침묵하지 않는다.

---

## 2. 화면

`mockups/v1.0_baseline.png` — 카드 3개.

```
┌ DEEPLY ─────────── 마이크 상태 ── 정지 배지 ─── 세션 · 오늘 잡은 클릭 47 ┐
├──────────────────────────────┬──────────────────────────────────────┤
│ 실시간 파형 (F2)              │  소리의 지도 (F4)                     │
├──────────────────────────────┤   ★ 방금 그 소리 + 판정 원            │
│ 스펙트로그램 (F3)             │  ─────────────────────────────────   │
│  + 모델이 주목한 부분          │  판정 기준 dial (F6) · 시험 성적표     │
└──────────────────────────────┴──────────────────────────────────────┘
```

옆 카드에 있던 숫자(SNR·배경 소음·이벤트 밀도·판정)는 전부 세 뷰 안으로 넣었다 —
곁가지를 다 지워도 정보가 남게 하기 위해서다.

**색 규칙**: Deeply Pink = 브랜드(상단 바·로고 줄 전용) · Teal = 검출/이 이벤트 · Deeply Black = 기존 체결음·본문.

---

## 3. 실행 형태 — 노트북 로컬

```
[Scarlett 2i2 + 7cm 마이크] ──96 kHz ch0──▶ Python (sounddevice)
                                              │ ring buffer, 1.0s window / 0.2s hop
                                              ▼
                                          HDTransysClassifier (DAL)
                                              │ probs · pair · embedding
                                              ▼  WebSocket (127.0.0.1)
                                          브라우저 화면 (표시 전용)
```

- **오디오와 모델은 Python이 독점한다.** 브라우저는 마이크를 열지 않는다 —
  getUserMedia가 96 kHz로 열린다는 보장이 없고, 48 kHz를 업샘플해도 학습 대역은 돌아오지 않는다.
- localhost라 인증서·VPN·사내 서버가 전부 필요 없다. 현장 네트워크가 죽어도 돈다.
- 실행은 launcher 하나: selftest → 96 kHz 실측 확인 → 추론 속도 측정 → 브라우저 자동 열기.

---

## 4. 엔진

`DAL.inference.hdtransys.HDTransysClassifier` (v4_e20) — 이미 존재하고 검증된 것을 그대로 쓴다.

| 항목 | 값 |
|---|---|
| 입력 | 96 kHz, ch0(7cm 마이크), 1.0초 window, 0.2초 hop |
| 출력 | `predict()` → `probs[others, click_1, click_2]` · `pair` / `finalize()` → 그 cycle 확정 |
| 판정 3단 | manifest `strictness_map` 의 검증된 레벨 **−1 / 0 / +1** ← dial과 1:1 |
| 속도 조절 | `load(folds=N)` — CPU 실시간이 빠듯하면 fold 수를 줄인다 |
| 지도 좌표 | `embed()` (2026-08-17 DAL에 추가) → fold0 penultimate 960-d, L2 정규화 |

**DAL 추가분은 add-only다.** `EfficientATInfer.forward_with_embedding()` 와
`HDTransysClassifier.embed()` 두 메서드만 새로 넣었고 기존 함수는 한 줄도 고치지 않았다.
`embed()` 는 postproc·immediate·detector·cycle 상태를 건드리지 않아 스트림 도중 아무 때나 불러도
판정이 달라지지 않는다. golden selftest 통과(4 cycle 판정·시각 일치, 오차 0.0 ms).

**PLC가 없으므로 cycle 경계를 사람이 만든다**: `대기 → 발표자가 시험 선언(ARM) → 분석 →
pair 확정 2.2초 안정 또는 10초 timeout 시 finalize → 대기`. 흐르는 환경음은 cycle에 넣지 않는다.

---

## 5. 스코프

| | 내용 |
|---|---|
| **필수** | F1~F6 |
| **스트레치** | 세션 리포트 PDF · 데이터 견적 곡선 화면 |
| **제외** | 현장 few-shot 학습 · 다채널 · 분류 세분화 · 온디바이스 |

---

## 6. 리스크

| 리스크 | 대응 |
|---|---|
| **도메인 갭** (학습 마이크 ≠ 데모 마이크) | 데모세트가 학습과 같은 7cm ch0 — 리허설에서 실측 확인 |
| **96 kHz 미개방** | 실제 stream rate가 96000이 아니면 **시작을 막는다**(조용히 리샘플하지 않는다) |
| **CPU 실시간** | 시작 시 p95 지연 측정 → 넘으면 fold 수 자동 축소 |
| **지도 원 = 판정** 오해 | 아래 미결 참조 — 현재 목업의 가장 큰 미해결 지점 |
| GradCAM 과장 | 모델과 검증된 방식으로 연결되기 전에는 "목업" 표기 유지 |

---

## 7. 미결 — 결정 필요

1. ~~지도의 원과 판정 기준~~ **결정(2026-08-17)**: 엔진에 이미 구현된 방식(`strictness_map`의
   tau)을 그대로 따르고, **원의 반지름은 그 tau를 단조로 매핑한 표시**로 쓴다. 즉 판정의 진짜 근거는
   score이고 원은 그것을 눈에 보이게 옮긴 것이다 — 별도 거리 기준을 새로 만들지 않는다.
2. **freeze를 무방비로 걸 것인가, 발표자가 시험을 선언한 구간에만 걸 것인가.**
   무방비 = 임팩트, 선언 = 안전(말소리에 화면이 얼면 민감도 과다를 자기가 광고하는 꼴).
3. 검출 카드/성적표를 미팅 후 리포트에 어디까지 남길지.

---

*설계 이력: `mockups/INDEX.md` — v01~v07, v1.0. 각 버전은 PNG와 그 PNG를 만든 스크립트가 짝으로 남아 있다.*
